apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f ../../docker-compose.yml
    kompose.version: 1.37.0 (HEAD)
  labels:
    io.kompose.service: admin-service
  name: admin-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: admin-service
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f ../../docker-compose.yml
        kompose.version: 1.37.0 (HEAD)
      labels:
        io.kompose.service: admin-service
    spec:
      containers:
        - env:
            - name: DB_DSN
              value: root:1234@tcp(mysql:3306)/service?charset=utf8mb4&parseTime=True&loc=Local
            - name: SERVER_PORT
              value: :8081
          image: hanwoo-admin-service:latest
          imagePullPolicy: Never
          name: admin-service
          ports:
            - containerPort: 8081
              protocol: TCP
      restartPolicy: Always
